namespace DSM.GatewayEngine
{
    public enum IdentityRole
    {
        Member = 0,
        Spectator = 1,
        Manager = 2,
        Administrator = 4,
        CIFANG = 5
        // CI FANG - Clever Idea For Accessing Nancy Gateway
        // CI FANG - CONTINOUS INTEGRATION ROOT
    }
}
